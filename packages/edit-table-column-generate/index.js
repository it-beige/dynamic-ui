import EditTableColumnGenerate from '../edit-table-generate/src/column.vue';

/* istanbul ignore next */
EditTableColumnGenerate.install = function (Vue) {
  Vue.component(EditTableColumnGenerate.name, EditTableColumnGenerate);
};

export default EditTableColumnGenerate;
